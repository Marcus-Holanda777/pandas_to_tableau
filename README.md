## Funçõe que exportam um dataframe do pandas para o tableau

* API que trata o arquivo .hyper

  > pip install tableauhyperapi

* API que faz a conexão com o tableau
  
  > pip install tableau_api_lib
